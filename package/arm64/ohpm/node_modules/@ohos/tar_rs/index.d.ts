/* tslint:disable */
/* eslint-disable */

/* auto-generated by NAPI-RS */

/**
 * Options for extracting files from a tar archive.
 *
 * # Fields
 *
 * * `file_path`: The path to the tar archive file.
 * * `out_path`: The path to the output directory where the extracted files will be saved.
 * * `strip`: An optional number of leading path components to strip from the extracted file paths.
 * * `file_list`: An optional list of file paths to extract from the tar archive.
 * If provided, only the files in this list will be extracted.
 */
export interface ExtractOptions {
    filePath: string
    outPath: string
    strip?: number
    fileList?: Array<string>
}

/**
 * Extracts files from a tar archive and saves them to the specified output directory.
 *
 * # Arguments
 *
 * * `options` - Options for extracting files.
 *   - `file_path`: The path to the tar archive file.
 *   - `out_path`: The path to the output directory where the extracted files will be saved.
 *   - `strip`: An optional number of leading path components to strip from the extracted file paths.
 *   - `file_list`: An optional list of file paths to extract from the tar archive. If provided, only the files in this list will be extracted.
 *
 * * `call_back` - Callback function to be called when extraction is complete.
 *
 * # Returns
 *
 * An `Result` indicating success or failure.
 *
 * # Errors
 *
 * If the tar archive file cannot be opened or read, or if the output directory cannot be created, an error will be returned.
 *
 * If the callback function cannot be created or called, an error will be returned.
 *
 * If any other error occurs during the extraction process, an error will be returned.
 */
export declare function extractRs(options: ExtractOptions, callBack: (...args: any[]) => any): void

/**
 *
 * This function lists the contents of a tar archive.
 * It takes a file path and a JavaScript function as parameters.
 * The JavaScript function is called for each entry in the tar archive.
 * The function opens the tar archive file, iterates through its entries,
 * and extracts the necessary information (path, size, type) for each entry.
 * It then calls the JavaScript function with the extracted data.
 * Finally, it prints the total time taken to list the contents of the tar archive.
 *
 * # Arguments
 *
 * * `file_path` - The path of the tar archive file.
 * * `onentry` - The JavaScript function to be called for each entry in the tar archive.
 *
 * # Returns
 *
 * A Result indicating whether the operation was successful.
 *
 */
export declare function listRs(filePath: string, onentry: (...args: any[]) => any): void
